Situation
1.[1, 2, 3]   // 没有0
2.[0, 1, 2]   // 有0
3.[0, 0, 0]   // 全为0
3.[]          // 为空

Similarity/Repeatability
1.非零元素相对位置保持不变

Solution
1. 暴力求解法， 遍历一遍记录下原始数组中零的个数以及将非零的元素填充到新的数组里，然后再将原数组的指向新的数组
（问题要求不可以拷贝数组不适用于该题）
2. 双指针法，使用一个指针遍历数组，在遍历的过程中，将非零值都赋值给非零指针指向的位置，然后非零指针加一，遍历完成后
再将数组中非零指针指向的位置后的所有内容置为0
