Situation
1.[1, 2, 3]   // 普通的情况，末尾加一不会导致进位
2.[0]         // 为0的情况
3.[1, 2, 9]   // 末尾加一会导致进位，影响前一位
4.[9, 9, 9]   // 末尾加一导致多次进位，影响了数组的长度

Similarity/Repeatability
1. 原数字为9的加1后会变成10，需要处理(每个元素只存储单个数字)，本位置会变成0，同时前一位需要加1
2. 原数字加1取余后本位置非0的表示操作结束，可以返回。

Solution
1. 数组里存的为整数，可以将数组加一后判断能否被10整除
