######  python recrusion 递归模板

def recrusion(level,Max,par1,par2...)：


     #####  recrusion teminator
     if level > Max:
         process_result
     return

     ####  process logic in current level
     process(level,data,...)

     ##### drill down
     self.recrusion(level + 1,par1,par2....)

     # reverse the current level status if needed



def  recrusion(level,Max,par1,par2,...):
    #### recrusion teminator

     if level > Max:
         process_result
     return

     #####  process logic in current level
    process(level,data,...

    ##### drill down
    self.recrusion(level + 1,par1,par2,...)

    #### reverse the current level status if needed